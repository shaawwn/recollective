import {useState, useEffect, useContext, useCallback} from 'react';

export const PlaylistContext = useContext();

export const PlaylistProvider = ({children}) => {

    const value = {
        add,
        remove,
    }
}